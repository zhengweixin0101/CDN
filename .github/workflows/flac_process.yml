name: Auto Process Music Files

on:
  workflow_dispatch:
  push:
    branches:
      - music

jobs:
  process:
    runs-on: ubuntu-latest
    environment: production
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: music

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install mutagen Pillow

      - name: Run music processor
        run: |
          python .github/scripts/process_flac.py

      - name: Commit and push new files
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add . ':!duplicates'
          git commit -m "🎵 Auto process new FLAC file(s)" || echo "No changes to commit"
          git push

      - name: Commit and create PR for duplicates
        run: |
          cd $GITHUB_WORKSPACE
          git checkout -b "duplicates-$(date +%s)"
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add duplicates
          git commit -m "⚠️ Duplicate FLAC files"
          git push origin HEAD
          gh pr create --title "Duplicate FLAC files" \
                       --body "以下文件与现有歌曲重复，请手动处理" \
                       --base music \
                       --head $(git rev-parse --abbrev-ref HEAD)